name: app_with_provider_template
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # UI
  responsive_framework: ^1.1.0


  # Architecture
  provider: ^6.0.5
  get_it: ^7.6.0

  # Persistence
  shared_preferences: ^2.2.0

  # Logging
  flutter_fimber: ^0.7.1

  # L10N
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/images/
